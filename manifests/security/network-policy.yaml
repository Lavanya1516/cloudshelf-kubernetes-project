apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  name: allow-web-traffic
  namespace: cloudshelf-namespace

spec:
  podSelector:
    matchLabels:
      app: cloudshelf-web

  policyTypes:
    - Ingress

  ingress:
    - ports:
        - protocol: TCP
          port: 80
